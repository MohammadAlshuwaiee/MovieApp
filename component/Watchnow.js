import styled from "styled-components";
import React from "react";

const ReviewsWrapper = styled.View`
  flex: 1;
  align-items: center;
  padding-top: 100px;
`;

const Watchnow = ({ navigation }) => {
  return <ReviewsWrapper></ReviewsWrapper>;
};

export default Watchnow;
